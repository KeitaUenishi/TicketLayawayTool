<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.product.mapper.LiveListMapper">

	<select id="findAll" resultType="com.example.product.domain.LiveList">
		SELECT
			dateId,
			place,
			remarks
		FROM ticketlayawaytool.live_list
	</select>

</mapper>
